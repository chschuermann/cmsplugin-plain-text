============
Installation
============

At the command line::

    $ easy_install cmsplugin-plain-text

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cmsplugin-plain-text
    $ pip install cmsplugin-plain-text
